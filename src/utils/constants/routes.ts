export const routes = {
  page: "/",
  home: "/*",
  notFoundPage: "*",
  dashboard: "/dashboard",
  preview: "/preview/:id",
};

// this contain all types of routes
export const ROUTES = {
  ...routes,
};

export default ROUTES;
